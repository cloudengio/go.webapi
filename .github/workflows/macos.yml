name: go-macos

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  test:
    uses: cloudengio/.github/.github/workflows/go-test-multimod.yml@main
    with:
      runner: |
        [ "self-hosted", "macos", "arm64" ]
      go-versions: |
        [ "1.27.x" ]
